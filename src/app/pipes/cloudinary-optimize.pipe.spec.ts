import { CloudinaryOptimizePipe } from './cloudinary-optimize.pipe';

describe('CloudinaryOptimizePipe', () => {
  it('create an instance', () => {
    const pipe = new CloudinaryOptimizePipe();
    expect(pipe).toBeTruthy();
  });
});
